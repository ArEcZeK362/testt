import localFont from 'next/font/local';

const streetbomber = localFont({ src: './Streetbomber.ttf' });
const stormgust = localFont({ src: './Storm Gust.otf' });
const pokemon = localFont({ src: './Pokemon.ttf' });

export  { streetbomber, stormgust, pokemon };
